#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    scanf("%d",&t);
    while(t--)
    {
        set<string>st;
        string s;
        while(cin>>s)
        {
            if(s == "*")
                break;
            else
                st.insert(s);
        }
        string frm,to;
while
    }
    return 0;
}
